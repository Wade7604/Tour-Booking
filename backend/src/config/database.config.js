// Collections name constants
const COLLECTIONS = {
  USERS: "users",
  ROLES: "roles",
  PERMISSIONS: "permissions",
  TOURS: "tours",
  DESTINATIONS: "destinations",
  BOOKINGS: "bookings",
  PAYMENTS: "payments",
};

// Default pagination
const PAGINATION = {
  DEFAULT_PAGE: 1,
  DEFAULT_LIMIT: 10,
  MAX_LIMIT: 100,
};

module.exports = {
  COLLECTIONS,
  PAGINATION,
};
